// Create an empty array called Students

// Create a function that adds a new student to the arrays

// Create a function that loops over the array of students and displays them on the page.

// Add a click handler.
  // When the button is clicked, get the name of the student from the input.
  // Use your function to add that student to the array
  // User your display function to show the new student list on the page.  
